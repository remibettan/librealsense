# Python dependencies for CUDA benchmark scripts
# Install with: pip install -r cuda_benchmark_requirements.txt

# Core dependencies
# Note: Using numpy<2 for compatibility with older matplotlib/pyrealsense2 builds
# If you have NumPy 2.x compatibility issues, run: pip install "numpy<2" --force-reinstall
numpy>=1.19.0,<2.0.0
matplotlib>=3.3.0

# GPU monitoring (NVIDIA GPUs only)
nvidia-ml-py3>=7.352.0

# RealSense Python bindings (required for cuda_benchmark.py with live camera)
# Note: pyrealsense2 should be built and installed with librealsense
# If not built with librealsense, install with:
# pip install pyrealsense2
